import 'package:flutter/material.dart';

// wraps the list widget and also the settings 
class Home extends StatelessWidget {
  const Home({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}